<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>SignIn</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      section {
        /* background-color: #b5c0d0; */
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        padding-top: 25px;

        padding-bottom: 10px;
      }
      section form {
        margin-top: 10px;
      }
      section form input {
        padding: 7px 60px;
        margin-bottom: 10px;
        border-radius: 10px;
        outline: 0;
        border: 0;
        border-radius: 2px solid black;
        background-color: #dddddd;
      }
      section form button {
        padding: 8px 130px;
        background-color: rgb(4, 4, 99);
        color: #fff;
        border: 0;
        outline: 0;
        border-radius: 10px;
      }
      a {
        list-style: none;
        text-decoration: none;
      }
    </style>
  </head>
  <body>
    <section>
      <h2>Sign In</h2>
      <% if (message) { %>
      <p><%= message %></p>
      <% } %>
      <form action="/user/signin" method="post">
        <input type="email" name="email" placeholder="Email" required /><br />
        <input
          type="password"
          name="password"
          placeholder="Password"
          required
        /><br /><br />
        <div
          class="g-recaptcha"
          data-sitekey="6Lfr2ywqAAAAADkZ3R-N6rIXWwrdm_CvswRxCZlR"
        ></div>
        <br />
        <button type="submit">Sign In</button>
      </form>
      <br />
      <!-- Sign-in Google Auth -->
      <!-- <a href="/auth/google">Sign In with Google</a> &nbsp&nbsp -->

      <a href="/user/forgot-password">Forgot Password?</a>
      <!-- Link to forgot password page -->
      <p>Don't have an account? <a href="/user/signup">Sign up</a></p>
    </section>
  </body>
</html>
